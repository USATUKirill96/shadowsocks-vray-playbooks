- name: Copy local files
  ansible.builtin.copy:
    src: "docker"
    dest: "~/nginx_conf"
  tags: [nginx, keys]

- name: Setting .env file
  template:
    src: "env.j2"
    dest: "~/nginx_conf/docker/.env"

- name: Run nginx
  import_tasks: nginx.yml
  tags: nginx

- name: Setup cert generation
  import_tasks: certbot.yml
  tags: keys